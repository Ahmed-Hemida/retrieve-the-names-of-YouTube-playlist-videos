# YouTube Playlist Video Name Extractor

This simple JavaScript script allows you to extract the names of videos from a YouTube playlist page and store them in an array. The extracted video names can be used for various purposes, such as creating a list of video titles for reference or analysis.

## Prerequisites

Before using this script, ensure you have the following:

- A web browser (tested on [Browser Name and Version])
- A YouTube playlist page loaded in the browser

## Usage

1. Open a web browser and navigate to the YouTube playlist page from which you want to extract video names.

2. Open the browser's developer console:
   - **Chrome**: Press `Ctrl+Shift+J` (Windows/Linux) or `Cmd+Option+J` (Mac).
   - **Firefox**: Press `Ctrl+Shift+K` (Windows/Linux) or `Cmd+Option+K` (Mac).
   - **Edge**: Press `Ctrl+Shift+I` (Windows/Linux) or `Cmd+Option+I` (Mac).

3. Copy the JavaScript code provided in this repository and paste it into the console.

4. Press `Enter` to execute the code. The script will extract the video names and display them in the console.

5. You can use the extracted video names for your specific needs, such as creating a list or performing further analysis.

## Example

Here's an example of how the script output might look:

```javascript
["Video Name 1", "Video Name 2", "Video Name 3", ...]
